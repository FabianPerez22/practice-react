Para iniciar front primero debes de entrar a la carpeta Farmacia y escribir npm install, crear el archivo .env y luego npm run dev.

Para iniciar el backend primero debes entrar a la carpeta farmaciaBackend, npm install, docker-compose up -d y por ultimo npm run dev
